# LLM / AI
langchain>=0.2
langchain-openai>=0.1



# Google Calendar
google-auth>=2.0
google-auth-oauthlib>=1.0
google-api-python-client>=2.0

# Config / infra
pydantic-settings>=2.0
python-dotenv>=1.0

# MongoDB
motor>=3.4
beanie>=1.26

# API / Worker
fastapi>=0.95
uvicorn>=0.23
redis>=5.0
celery>=5.3
requests>=2.31

# Interface
streamlit>=1.35
streamlit-autorefresh>=1.0
playwright>=1.60.0

# Áudio
pyaudio>=0.2.14

# Gravação ativa
# ffmpeg é uma dependência de sistema usada para decodificar/formatar áudio
# gerado pelo recorder e pelo Whisper local. Instale-o antes de usar o projeto:
#   macOS:   brew install ffmpeg
#   Linux:   sudo apt install ffmpeg
#   Windows: choco install ffmpeg
#            (ou baixe em https://www.gyan.dev/ffmpeg/builds/ e adicione ao PATH)
