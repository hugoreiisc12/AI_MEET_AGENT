[python]
# Raiz do projeto — garante que imports como "domain.xxx", "config.xxx" sejam encontrados
python_path = ["."]

# Interpretador do ambiente virtual
venv_name = "venv"

[rules]
# Silencia erros de imports de libs opcionais (pyannote, torch) que são lazy-loaded
missing_module_source = "ignore"
